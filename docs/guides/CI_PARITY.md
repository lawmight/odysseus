# CI parity: fork vs upstream

_Auto-generated by [`scripts/ci-parity-report.sh`](../../scripts/ci-parity-report.sh) at 2026-06-04 06:07 UTC. Re-run after upstream syncs CI workflows._

| Repository | GitHub |
|------------|--------|
| Upstream | `pewdiepie-archdaemon/odysseus` |
| Fork | `lawmight/odysseus` |

## Workflow files on `main` (source of truth)

GraphQL tree for `.github/workflows/` — only files present on the default branch count for merge gates.

### Upstream

- `issue-description-check.yml`
- `pr-description-check.yml`

### Fork

- `ci.yml`
- `docker.yml`
- `issue-description-check.yml`
- `pr-description-check.yml`

## Registered Actions workflows (may include stale entries)

The Actions API can list workflows that no longer exist on `main`. Prefer the tree above when syncing.

### Upstream (registered)

- `.github/workflows/ci.yml`
- `.github/workflows/container-scan.yml`
- `.github/workflows/dependency-review.yml`
- `.github/workflows/issue-description-check.yml`
- `.github/workflows/pr-description-check.yml`
- `.github/workflows/pytest-smoke.yml`
- `.github/workflows/secret-scan.yml`
- `.github/workflows/workflow-security.yml`

### Fork (registered)

- `.github/workflows/ci.yml`
- `.github/workflows/docker.yml`
- `.github/workflows/issue-description-check.yml`
- `.github/workflows/pr-description-check.yml`

## Gate mapping

| Gate | Upstream `main` | Fork `main` | Fork covers upstream? |
|------|-----------------|-------------|------------------------|
| PR description bot | `pr-description-check.yml` | same file (shared script) | yes |
| Issue description bot | `issue-description-check.yml` | same file; fork issues **false** | N/A on fork if issues off |
| Full `pytest` | manual per CONTRIBUTING | `ci.yml` → `test` | yes (stricter) |
| Python/JS syntax | manual | `ci.yml` → `syntax` | yes |
| Secret scan | manual | `ci.yml` → `secrets` | yes |
| Docker compose config | manual | `docker.yml` → `docker-config` | yes |
| Docker image build | manual | `docker.yml` → `docker-build` | yes |

**Implication:** Green CI on the fork is a superset of upstream’s automated expectations on `main` today. Upstream PRs still require a complete PR description and (for agents) issue-first workflow on `pewdiepie-archdaemon/odysseus`.

## Fork branch protection (documented)

From [CONTRIBUTING.md](../../CONTRIBUTING.md): required checks on `lawmight/odysseus` `main` PRs:

- `test`
- `syntax`
- `secrets`
- `docker-config`
- `docker-build`
- `Check PR description` (workflow name: `ci / PR description check`)

## Sync policy

1. Re-run `bash scripts/ci-parity-report.sh` after merging upstream `main`.
2. If upstream adds new workflow files to `main`, port them in a focused `chore: sync upstream CI` PR.
3. Do **not** copy workflows that appear only in the Actions API but not in upstream’s `main` tree (likely stale).

## Local preflight

Run `bash scripts/ci-preflight.sh` before pushing. See [UPSTREAM_PR_GUIDELINES.md](./UPSTREAM_PR_GUIDELINES.md).
